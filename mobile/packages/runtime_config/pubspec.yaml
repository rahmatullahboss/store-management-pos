name: store_companion_runtime_config
description: Fail-closed environment and endpoint configuration for Store Companion.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0
