name: store_companion_sync_engine
description: Pure operation state and retry invariants for Store Companion sync.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  store_companion_api_client:
    path: ../api_client
