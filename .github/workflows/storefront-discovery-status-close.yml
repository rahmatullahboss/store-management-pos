name: Retired Storefront Discovery Closeout

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Discovery closeout is complete; this workflow is intentionally inert."
